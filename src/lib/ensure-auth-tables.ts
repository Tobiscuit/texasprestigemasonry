/**
 * Auth table bootstrapping - PLACEHOLDER
 * 
 * Previously used pg (Postgres) to create auth tables.
 * Now using BetterAuth memory adapter until D1 is wired up.
 * When D1 is ready, this file should use Drizzle migrations instead.
 */

export const ensureAuthTablesReady: Promise<void> = Promise.resolve();
